version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: minimusic_app
    environment:
      DATABASE_URL: "postgresql://minimusic:minimusic123@minimusic-db-li1xgw:5432/minimusic?schema=public"
      ENCRYPTION_KEY: "MiniMusicSecureKey123456789012"
      MINIMAX_API_URL: "https://api.minimax.io"
      MINIMAX_API_KEY: "${MINIMAX_API_KEY:-}"
      PORT: "3001"
      FRONTEND_URL: "http://localhost:5173"
    ports:
      - "3002:3001"
    volumes:
      - ./backend:/app/backend
      - ./frontend:/app/frontend
